apiVersion: v1
kind: Service
metadata:
  labels:
    app: inference-svc
  name: inference-svc
spec:
  ports:
  - name: 8080-8080
    port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    app: inference-api
  type: NodePort
status:
  loadBalancer: {}
